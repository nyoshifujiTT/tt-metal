name: "[internal] Single-card Frequent model host-side (device-free) tests impl"

on:
  workflow_call:
    inputs:
      build-artifact-name:
        required: true
        type: string
      wheel-artifact-name:
        required: true
        type: string
      docker-image:
        required: true
        type: string
      models-to-run:
        required: false
        type: string
        default: "all"

jobs:
  load-test-matrix:
    runs-on: ubuntu-slim
    outputs:
      matrix: ${{ steps.apply-filters.outputs.matrix }}
    env:
      TESTS_YAML_PATH: ./tests/pipeline_reorg/single_card_ttnn_models_frequent_hostside_tests.yaml
    steps:
      - name: Checkout the repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        timeout-minutes: 20
        with:
          sparse-checkout: |
            .github
            tests/pipeline_reorg
          sparse-checkout-cone-mode: true
      - name: Install dependencies
        run: pip3 install PyYAML
      - name: Verify test timeouts against budget
        run: |
          set -e
          python3 .github/scripts/utils/verify_time_budget.py \
            ${{ env.TESTS_YAML_PATH }} \
            ./.github/time_budget.yaml \
            "frequent"
      - name: Build unified test matrix
        id: build-matrix
        run: |
          python3 .github/scripts/utils/prepare_test_matrix.py \
            ${{ env.TESTS_YAML_PATH }} \
            ALL_SKUS_IN_TESTS \
            ./.github/sku_config.yaml
      - name: Apply model filter
        id: apply-filters
        run: |
          MATRIX='${{ steps.build-matrix.outputs.matrix }}'
          SEL='${{ inputs.models-to-run }}'
          if [ -z "${SEL}" ] || [ "${SEL}" = "all" ]; then
            FILTERED="${MATRIX}"
          elif echo "${SEL}" | jq -e 'type == "array"' >/dev/null 2>&1; then
            FILTERED=$(echo "${MATRIX}" | jq -c --argjson sel "${SEL}" '[.[] | select(.model | IN($sel[]))]')
          else
            echo "::error::models-to-run must be 'all' or a JSON array of model names"
            exit 1
          fi
          if [ "$FILTERED" = "[]" ]; then
            echo "::warning::No host-side tests found for models-to-run=${SEL}. Pipeline will be skipped."
            echo "matrix=[]" >> "$GITHUB_OUTPUT"
          else
            echo "matrix<<EOF" >> "$GITHUB_OUTPUT"
            echo "$FILTERED" >> "$GITHUB_OUTPUT"
            echo "EOF" >> "$GITHUB_OUTPUT"
          fi

  single-card-ttnn-models-frequent-hostside-tests:
    needs: load-test-matrix
    if: ${{ needs.load-test-matrix.outputs.matrix != '[]' }}
    strategy:
      fail-fast: false
      matrix:
        test-group: ${{ fromJson(needs.load-test-matrix.outputs.matrix) }}
    name: ${{ matrix.test-group.name }}
    # CPU runner (cpu_medium): no Tenstorrent device is attached. These tests
    # import ttnn from the build but never open a device, so options intentionally
    # omit --device /dev/tenstorrent.
    runs-on: ${{ matrix.test-group.runs_on }}
    container:
      image: ${{ inputs.docker-image || 'docker-image-unresolved!' }}
      env:
        TT_METAL_HOME: /work
        PYTHONPATH: /work
        LD_LIBRARY_PATH: /work/build/lib
        LOGURU_LEVEL: INFO
        GTEST_OUTPUT: xml:/work/generated/test_reports/
        PYTEST_CMD: pytest
      volumes:
        - ${{ github.workspace }}/docker-job:/work
    defaults:
      run:
        shell: bash
        working-directory: /work
    steps:
      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        timeout-minutes: 20
        with:
          submodules: recursive
          path: docker-job
      - name: Setup Job
        uses: ./docker-job/.github/actions/setup-job
        timeout-minutes: 10
        with:
          build-artifact-name: ${{ inputs.build-artifact-name }}
          wheel-artifact-name: ${{ inputs.wheel-artifact-name }}
      - name: ${{ matrix.test-group.name }}
        timeout-minutes: ${{ matrix.test-group.timeout }}
        uses: ./docker-job/.github/actions/run-with-log
        with:
          log-file: generated/test_logs/${{ matrix.test-group.name }}.log
          run: |
            mkdir -p generated/test_reports
            ${{ matrix.test-group.cmd }}
      - uses: tenstorrent/tt-metal/.github/actions/upload-artifact-with-job-uuid@main
        timeout-minutes: 10
        if: ${{ !cancelled() }}
        with:
          prefix: "test_reports_"
      - name: Generate gtest annotations on failure
        uses: tenstorrent/tt-metal/.github/actions/generate-gtest-failure-message@main
        if: ${{ failure() }}
